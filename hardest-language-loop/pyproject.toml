[project]
name = "hardest-language-loop"
version = "0.1.0"
description = "Web-monitored agent loop for hardest programming language discovery"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
